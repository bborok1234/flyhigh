# Basic Adapter Rubric

The adapter fixture passes when:

- `scripts/generate-adapters` creates Claude and OpenCode adapter files;
- Claude command wrappers exist for every Flyhigh skill;
- generated adapter files include the generated marker;
- canonical Flyhigh skills remain the source of truth.

