# Basic Skill Contract Rubric

The skill contract fixture passes when:

- every skill has YAML frontmatter;
- every skill name starts with `flyhigh-`;
- every skill has an actionable description;
- every skill includes workflow, outputs, and stop condition sections.

