# Basic Python Unittest Bootstrap Fixture

Expected behavior:

- Stack includes Python.
- Directories include `src` and `tests`.
- Test command uses stdlib unittest when pytest is not configured.
- `src` layout adds `PYTHONPATH=src`.
