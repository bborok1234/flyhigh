#!/usr/bin/env python3
import subprocess
from pathlib import Path


def main() -> int:
    root = Path(__file__).resolve().parents[1]
    subprocess.run(["scripts/generate-adapters"], cwd=root, check=True)
    print("claude surface generated")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())

